name: Optimizar guardado de conteos

on:
  push:
    paths:
      - '.github/workflows/optimizar-guardado-conteos.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Optimizar backend y mensaje
        run: |
          python - <<'PY'
          from pathlib import Path

          p = Path('apps-script/Conteos.gs')
          s = p.read_text(encoding='utf-8')
         